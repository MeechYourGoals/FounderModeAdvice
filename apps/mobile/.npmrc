# Expo's dependency tree (and the shared workspace) resolve cleanly under npm's
# legacy peer-dependency algorithm. Scoped to this app so the root web install is
# left untouched.
legacy-peer-deps=true
