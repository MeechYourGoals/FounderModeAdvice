You are operating an authenticated browser for Founder Mode Advice. First read docs/app-store/EXTERNAL_SETUP_VALUES.json, APP_STORE_METADATA.md, APP_PRIVACY_MATRIX.md, REVENUECAT_MAPPING.md, APP_REVIEW_NOTES.md and BLOCKERS.md. Existing confirmed repository values are app “Founder Mode Advice”, legal-entity claim “Saint Marlo Labs LLC”, bundle ID/scheme `com.foundermodeadvice.app`, Expo slug `founder-mode-advice`, production URL `https://foundermodeadvice.com`, support `https://foundermodeadvice.com/contact`, privacy `/privacy-policy`, terms `/terms-of-service`, deletion `/account-deletion`, contact `CA@saintmarlolabs.com`. Commercial identifiers are NOT confirmed: repository candidates are products `seed_monthly` and `series_z_monthly`, offering `default`, and conflicting entitlement candidates. Do not create or normalize them.

Inspect every existing record before creating anything. Reconcile names, bundle IDs, teams, SKUs, Apple numeric IDs, Expo owner/project, RevenueCat project/app and legal ownership; never create duplicates. After each section re-open it and verify every saved value. Keep a ledger with DONE, BLOCKED, USER ACTION REQUIRED and FAILED plus non-secret evidence. Never disclose passwords, 2FA codes, `.p8` contents, private keys or sensitive account data.

PAUSE for explicit user confirmation before creating immutable bundle IDs/SKUs/product IDs; accepting agreements; tax/banking changes; generating/uploading keys; finalizing prices/trials/regions/availability; buying/upgrading; submitting to review; or public release. Browser automation cannot upload files: stop at an explicit USER UPLOAD checkpoint for screenshots, IAP review screenshot and `.p8`, then continue only after confirmation.

REVENUECAT WORK:
1. Open RevenueCat and inspect projects/apps for this bundle. Verify ownership and Apple app linkage; do not create if one exists.
2. Record the existing RevenueCat project/app IDs and Apple public SDK key name (never print secret key values). Confirm Apple In-App Purchase Key + Issuer ID status. An optional App Store Connect API key is justified only for product import; pause before key upload.
3. Inspect Apple products, entitlements, offerings, packages, current offering, paywall and Customer Center. Compare against the unconfirmed candidates and report differences. PAUSE for the owner’s exact mapping decision.
4. After approval only, import existing Apple products rather than duplicating, attach exact entitlements, monthly/annual packages only when confirmed, set current offering, and configure paywall/Customer Center with Restore, management, price/period/renewal/trial, Privacy and Terms.
5. Inspect Apple Server Notifications V2 production and sandbox URLs. Configure RevenueCat’s documented URL only after App Store reconciliation. If a custom webhook is selected, require a server endpoint with authorization, raw-body verification where supported, replay resistance and idempotency; never place a private key client-side.
6. Add RevenueCat sandbox access/testers and validate offering load, cancellation, purchase, restore, second device, renewal, expiry, grace and revocation. Record actual results; do not claim readiness without them.
7. Return the non-secret exact IDs and variable names for EXTERNAL_SETUP_VALUES.json. Secret placement: Apple public SDK key → EAS production `FMA_REVENUECAT_IOS_API_KEY`; RevenueCat secret → Supabase Edge Function secret `REVENUECAT_API_KEY`.
