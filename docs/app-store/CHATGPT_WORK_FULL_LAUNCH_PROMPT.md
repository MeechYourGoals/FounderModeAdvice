You are operating an authenticated browser for Founder Mode Advice. First read docs/app-store/EXTERNAL_SETUP_VALUES.json, APP_STORE_METADATA.md, APP_PRIVACY_MATRIX.md, REVENUECAT_MAPPING.md, APP_REVIEW_NOTES.md and BLOCKERS.md. Existing confirmed repository values are app “Founder Mode Advice”, legal-entity claim “Saint Marlo Labs LLC”, bundle ID/scheme `com.foundermodeadvice.app`, Expo slug `founder-mode-advice`, production URL `https://foundermodeadvice.com`, support `https://foundermodeadvice.com/contact`, privacy `/privacy-policy`, terms `/terms-of-service`, deletion `/account-deletion`, contact `CA@saintmarlolabs.com`. Commercial identifiers are NOT confirmed: repository candidates are products `seed_monthly` and `series_z_monthly`, offering `default`, and conflicting entitlement candidates. Do not create or normalize them.

Inspect every existing record before creating anything. Reconcile names, bundle IDs, teams, SKUs, Apple numeric IDs, Expo owner/project, RevenueCat project/app and legal ownership; never create duplicates. After each section re-open it and verify every saved value. Keep a ledger with DONE, BLOCKED, USER ACTION REQUIRED and FAILED plus non-secret evidence. Never disclose passwords, 2FA codes, `.p8` contents, private keys or sensitive account data.

PAUSE for explicit user confirmation before creating immutable bundle IDs/SKUs/product IDs; accepting agreements; tax/banking changes; generating/uploading keys; finalizing prices/trials/regions/availability; buying/upgrading; submitting to review; or public release. Browser automation cannot upload files: stop at an explicit USER UPLOAD checkpoint for screenshots, IAP review screenshot and `.p8`, then continue only after confirmation.

FULL LAUNCH WORK (execute in order, never skip blockers):
1. Reconcile Apple Developer/App Store Connect, Expo/EAS and RevenueCat ownership and existing records. Capture non-secret team/owner/project/app IDs. Stop on any bundle/name/legal mismatch.
2. Perform every Apple step in CHATGPT_WORK_APP_STORE_CONNECT_PROMPT.md through TestFlight setup, including agreements/tax/banking/DSA status inspection, bundle/capabilities, version/categories/age/privacy/export/content rights/review information, subscriptions, localizations, notifications and upload checkpoints.
3. Perform every RevenueCat step in CHATGPT_WORK_REVENUECAT_PROMPT.md, reusing—not duplicating—Apple products. Verify Apple IAP key/Issuer, optional ASC API key justification, products, entitlement, offering/packages/current offering, paywall/Customer Center, restore, notification URL and sandbox.
4. Open Expo, inspect owner/projects for slug and bundle, and reconcile before `eas init`. After user approval link the existing project. Set EAS development/preview/production variables using secure inputs: production `FMA_WEB_URL=https://foundermodeadvice.com`, public Apple SDK key in `FMA_REVENUECAT_IOS_API_KEY`, optional OneSignal app ID. Never store RevenueCat private keys in EAS client config.
5. Inspect/manage Apple distribution certificate and provisioning profile, then run the production EAS build only with explicit paid-build authorization. Verify build logs contain production host and no Test Store key/localhost. Submit to TestFlight only with explicit authorization. Capture the numeric ASC Apple ID for future `ascAppId`.
6. Run the complete 22-scenario RELEASE_CHECKLIST on the exact TestFlight build and all three device classes. USER UPLOAD the actual screenshots and paywall review image. Re-open every metadata/privacy/IAP field.
7. Stop before App Review submission and public release. Present the final ledger, unresolved decisions and rollback. Never claim Apple approval is guaranteed.
