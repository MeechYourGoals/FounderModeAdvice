# Local env files — EAS environment variables are the source of truth for builds
.env
.env.*
!.env.example

# Store credentials — never commit
play-service-account.json
*.p8
*.p12
*.mobileprovision

# Generated native projects (CNG/prebuild) and build output
ios/
android/
dist/
node_modules/
.expo/
