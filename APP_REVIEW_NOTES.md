# App Review Notes — Founder Mode Advice

Paste the section below into **App Store Connect → App Review Information → Notes** before submitting via Despia. These notes directly address App Review Guideline §4.2 (minimum functionality / "web wrapper" rejections).

---

## Test account
- Email: _(create a fresh demo account before submission)_
- Password: _(provide here)_
- Sample saved insights are pre-loaded on this account.

## Native iOS features (Guideline §4.2)

Founder Mode Advice is built as a native iPhone experience using the Despia runtime. The following native capabilities are tied directly to the core user journey:

1. **Bottom tab navigation** — Home, Saved, Ask, Profiles, Profile. Tabs use iOS-style haptic feedback and safe-area insets. No web-style top navigation appears in the installed app.
2. **Native share sheet** — Saved insights and analysis results invoke the iOS share sheet through the Despia `share://` URL scheme. Tap the share icon on any Saved item or analysis result to verify.
3. **Haptic feedback** — Triggered on tab switches, save actions, analyze submissions, and share completions.
4. **Push notifications** — Optional daily founder prompts, plan reminders, and product updates. Toggles live under Settings → Notifications. Users explicitly opt in; preferences are stored server-side and respected immediately.
5. **Native in-app subscriptions** — Premium founder insights are gated by RevenueCat / Apple IAP in the installed app. Restore Purchases is available from Account and Settings.
6. **Deep linking** — Shared links open the corresponding analysis or saved item directly inside the installed app (`com.foundermodeadvice.app://`).
7. **Offline access** — The **Saved** tab and the **most recently viewed analysis** remain fully usable without network connectivity. An "Offline — showing saved content" banner appears when the device is offline. To verify: enable Airplane Mode, open the Saved tab, then open any cached item.

## Lasting utility
- Each user maintains a personalized library of founder analyses tied to their business profile (industry, stage, focus areas).
- Analyses are generated by Google Gemini Flash from YouTube transcripts and persist across sessions.
- Saved items and folders are user-curated, not shared web content.

## Account deletion
A self-serve account deletion flow is available at **Settings → Account & data → Delete account**, or directly at `/account-deletion`. Deletion removes all user data within 30 days.

## Subscription
Two auto-renewing monthly subscriptions, processed through Apple IAP via RevenueCat:
- **The C-Suite** — $9.99/month
- **The Boardroom** — $19.99/month

Restore Purchases is available under Settings → Subscription.

## Privacy
Privacy Policy: https://foundermodeadvice.com/privacy-policy
Terms of Service: https://foundermodeadvice.com/terms-of-service

---

## Submission checklist (internal — do not paste)

- [ ] Demo account credentials filled in above
- [ ] OneSignal App ID + REST API key added as Supabase secrets (`ONESIGNAL_APP_ID`, `ONESIGNAL_REST_API_KEY`)
- [ ] APNs `.p8` key uploaded to OneSignal dashboard
- [ ] `pg_cron` job scheduled to invoke `send-daily-prompt` daily at 13:00 UTC (see snippet below)
- [ ] Despia bundle ID `com.foundermodeadvice.app` registered in Apple Developer portal
- [ ] App Store Connect listing created with screenshots, description, keywords
- [ ] TestFlight build approved and tested with internal users
- [ ] Subscription products created in App Store Connect and linked to RevenueCat
- [ ] RevenueCat entitlement IDs match `src/types/subscription.ts` and `sync-revenuecat-subscription`
- [ ] Confirm no Stripe checkout/portal appears inside the iOS installed app
- [ ] Confirm `delete-user-account` removes Supabase user data and storage objects for the demo user

### Daily prompt cron snippet (run via Supabase SQL editor once OneSignal keys are set)

```sql
select cron.schedule(
  'send-daily-founder-prompt',
  '0 13 * * *',
  $$
  select net.http_post(
    url := 'https://rmkvryjmxvmueiyqsgar.supabase.co/functions/v1/send-daily-prompt',
    headers := '{"Content-Type": "application/json", "apikey": "<SUPABASE_ANON_KEY>"}'::jsonb,
    body := '{}'::jsonb
  );
  $$
);
```
